{ 
    "🥸" .A
    "😡" .B
}
"<>" .A
"<>" .B
"  " .Z

[] $Z +
   $Z +
   $Z +
   $Z +
   $Z +
   $Z +
   $Z + .linea

0 .index
$linea len -1 + .index2
1 .delta
:oioi
    $linea $index $A set
    $linea $index2 $B set
    $linea println
    150 sleep
    $linea $index $Z set
    $linea $index2 $Z set
    $index $delta + .index
    $index2 $delta -1 * + .index2
    $index 0 < @vuelta
    $index $linea len < @oioi
:vuelta

$delta -1 * .delta 
$index $delta + .index
$index2 $delta -1 * + .index2

@oioi
